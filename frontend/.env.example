# NyayaMitra frontend env (Vite reads these at BUILD time — rebuild after changes).
# Canonical variable is VITE_API_URL. VITE_API_BASE_URL is a deprecated alias
# still honoured by src/lib/utils.ts for existing docker-compose builds.
VITE_API_URL=http://localhost:8000
